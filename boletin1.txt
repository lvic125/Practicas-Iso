Boletín 1. 

1. 	pwd

2. 	cd /etc
	pwd
	
3.	ls -l -d a*

4. 	cd
	ls ../../bin/???? ../../sbin/???? 
ALT find ../../bin/ ../../sbin/ -name "????"

5. 	mkdir configuracion
	cp -a /etc/ /home/alumno/configuracion/

6. 	find -type d

7. 	find /home/alumno/configuracion/etc/ -type f -size -100k -exec rm -fr {} \; 

8.	find . -maxdepth 1 -type f -printf "Nombre %p, tamaño: %s bytes\n"

9.	mv configuracion/ /home/alumno/copia_etc
	
10. No, porque hay elementos en el directorio 
	rm -fr /home/alumno/copia_etc/

11. find . -name ".*"

12. find /usr/ -name "[[:upper:]]*[0-9]"

13. find /usr -name "*[!0-9]"

14. find /etc/ /usr/bin/ /usr/sbin/ -type f -mtime +7

15.	find ~alumno/ -newer ~/.bash_profile 

16. mkdir temp 
	find /etc -type f -size +5k -size -30k -name "g*" -exec cp {} ~alumno/temp/ \; -printf "Copiado %f de %s bytes\n" 2>/dev/null 
	
17. find ~alumno/ -type f -perm /g=rwx,o=rwx -exec chmod 600{} \;

18.	find /usr/bin/ /usr/sbin/ -type f -user root ! -perm -u=w -printf "Fichero: %p, permisos: %M\n"

